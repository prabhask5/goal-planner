{
  "name": "stellar-focus-extension",
  "version": "1.0.0",
  "description": "Firefox extension to block distracting websites during Stellar focus sessions",
  "type": "module",
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@supabase/supabase-js": "^2.45.0",
    "@types/webextension-polyfill": "^0.10.7",
    "typescript": "^5.3.0",
    "webextension-polyfill": "^0.12.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "webextension-polyfill": "^0.12.0"
  }
}
